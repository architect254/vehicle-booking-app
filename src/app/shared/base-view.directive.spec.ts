import { BaseViewDirective } from './base-view.directive';

describe('BaseViewDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseViewDirective();
    expect(directive).toBeTruthy();
  });
});
